public  function add_text($type,$name,$label,$required,$logics){
    return [
    'type'=> $type,
    "name"=> $name,
    "label"=> $label,
    "required"=> $required,
    "logics"=> $logics,
   ];}
   